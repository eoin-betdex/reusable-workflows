name: Reusable workflow example - AWS STS Assume Role

on:
  workflow_call:
    inputs:
      aws_region:
        required: false
        type: string
        default: "eu-west-2"
      aws_role_to_assume:
        required: false
        type: string

permissions:
  id-token: write
  contents: read
  pull-requests: write

jobs:
  AssumeRole:
    runs-on: ubuntu-latest
    steps:
      - name: Git clone the repository
        uses: actions/checkout@v3
      - name: configure aws credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          role-to-assume: ${{ inputs.aws_role_to_assume }}
          aws-region: ${{ inputs.aws_region }}
          role-skip-session-tagging: true
      - name: Do something afte assume role
        run: |
          echo "done"
